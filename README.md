# Playing around with React/GraphQL 
# Free GraphQL API https://rickandmortyapi.com/graphql#

# How to install
1. Clone repo.
2. npm install
3. goto http://localhost:<PORT>/  (Whatever port u specify)
